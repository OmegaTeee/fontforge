# Development tooling — kept separate from requirements.txt so production
# installs (and the MCP server) stay minimal.
#
# Install with:  venv/bin/pip install -r requirements-dev.txt

-r requirements.txt

ruff>=0.6.0
pytest>=8.0.0
